#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

git status --short --branch

cat <<'MSG'

Review the status above before committing or pushing.

Typical flow:
  git add -- <files>
  git commit -m "Update dotfiles"
  git push
MSG
